---
- name: User creation
  hosts: dev
  become: yes
  vars_files:
    user_list.yaml
  tasks:
    - name: User creation play
      ansible.builtin.user:
        name: "{{ item.name }}"
        group: "{{ item.job }}"
        state: present
      loop: "{{ users }}"
